/**
 * Read a file and returns its content. This supports different file encoding.
 */
export declare function readFile(fileName: string): string | null;
/**
 * Parse a JSON string and optionally remove comments from it first.
 */
export declare function parseJson(content: string, stripComments?: boolean): any;
export declare function readJsonFile(path: string): any;
